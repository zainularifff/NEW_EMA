export default function Dashboard() {
  return (
    <div className="ema-panel p-4">
      <h2 className="ema-page-title">Dashboard</h2>
      <p className="ema-page-subtitle">
        Fresh EMA System interface foundation is ready. Next we rebuild Hardware
        Inventory from API and functions.
      </p>
    </div>
  );
}